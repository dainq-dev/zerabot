{
  "name": "api-bridge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --env-file ../../.env --watch src/index.ts",
    "start": "bun run --env-file ../../.env src/index.ts",
    "db:migrate": "bun run --env-file ../../.env src/db/migrate.ts"
  },
  "dependencies": {
    "@zerobot/shared": "workspace:*",
    "smol-toml": "^1.3.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.14.0"
  }
}
